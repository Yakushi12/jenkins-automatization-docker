---

- hosts: default
  become: true
  gather_facts: no
  roles:
    - { role: docker, tags: jenkins-install }
    - { role: jenkins_role }

- hosts: Jenkins
  become: true
  gather_facts: no
  roles:
    - { role: jenkins_role }

...
