pipeline {
  agent {
      label "master"
  }

  stages {
    stage("clone code") {
      steps {
        script {
          git credentialsId: "{{ credentials }}",
          branch: "{{ targetBranch }}",
          url: "{{ gitCIurl }}";
        }
      }
    }
    stage('Create Packer AMI') {
      steps {
        dir('Terraform/Dev') {
          sh("terraform init")
          sh("terraform apply -auto-approve")
        }
      }
    }
  }

  post {
    cleanup {
      cleanWs()
    }
  }
}
